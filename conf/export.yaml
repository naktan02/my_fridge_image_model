export:
  format: tflite
  imgsz: 640
  nms: true
  int8: false            # ← 먼저 FP32로 성공 확인 후, 나중에 true로 재시도
  data: ./configs/data.yaml
  device: cpu
  use_direct_tflite: true
# fraction 키는 제거(버전 의존적이라 무시되는 경우 많음)

manifest:
  postprocess:
    score_thresh: 0.25
    nms_iou: 0.5
    max_det: 100
    has_builtin_nms: true
  data_version: "v0.3"
Q