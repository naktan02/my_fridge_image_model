# Python 가상환경
.venv/

# 훈련 결과물
runs/

# TFLite 모델 및 체크섬 파일
release/*.tflite
release/CHECKSUMS.txt

# 파이썬 캐시
__pycache__/
*.pt

# --- 데이터 폴더 (가장 중요!) ---
# 'data' 라는 이름의 폴더 무시
/data/

# 'my_fridge-' 로 시작하는 모든 폴더 무시 (my_fridge-1, my_fridge-2 등)
/my_fridge-*/

# 'datasets' 라는 이름의 폴더 무시
/datasets/

# '_temp_' 로 시작하는 모든 임시 폴더 무시
/_temp_*/

.env